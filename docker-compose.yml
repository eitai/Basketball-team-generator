services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: basketball_dev
      POSTGRES_USER: basketball
      POSTGRES_PASSWORD: basketball
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
